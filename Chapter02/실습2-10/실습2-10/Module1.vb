
Option Explicit Off

Module Module1

    Sub Main()
        Dim aInt As Integer
        'Option Explicit Off  선언으로 정의되지 않아도 사용 가능

        aPublic = 30
        aInt = 50
        Console.WriteLine("aPublic = {0}, aInt = {1}", aPublic, aInt)
        Console.ReadLine() '화면을 멈춘다
    End Sub

End Module
